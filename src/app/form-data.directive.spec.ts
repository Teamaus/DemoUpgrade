import { FormDataDirective } from './form-data.directive';

describe('FormDataDirective', () => {
  it('should create an instance', () => {
    const directive = new FormDataDirective();
    expect(directive).toBeTruthy();
  });
});
