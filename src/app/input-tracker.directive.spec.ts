import { InputTrackerDirective } from './input-tracker.directive';

describe('InputTrackerDirective', () => {
  it('should create an instance', () => {
    const directive = new InputTrackerDirective();
    expect(directive).toBeTruthy();
  });
});
