import { InHtmlDirective } from './in-html.directive';

describe('InHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new InHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
