import { FGFactoryDirective } from './fgfactory.directive';

describe('FGFactoryDirective', () => {
  it('should create an instance', () => {
    const directive = new FGFactoryDirective();
    expect(directive).toBeTruthy();
  });
});
