import { ApiKeysDirective } from './api-keys.directive';

describe('ApiKeysDirective', () => {
  it('should create an instance', () => {
    const directive = new ApiKeysDirective();
    expect(directive).toBeTruthy();
  });
});
