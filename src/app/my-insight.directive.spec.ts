import { MyInsightDirective } from './my-insight.directive';

describe('MyInsightDirective', () => {
  it('should create an instance', () => {
    const directive = new MyInsightDirective();
    expect(directive).toBeTruthy();
  });
});
