import { ApiGetDirective } from './api-get.directive';

describe('ApiGetDirective', () => {
  it('should create an instance', () => {
    const directive = new ApiGetDirective();
    expect(directive).toBeTruthy();
  });
});
