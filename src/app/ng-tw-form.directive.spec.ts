import { NgTwFormDirective } from './ng-tw-form.directive';

describe('NgTwFormDirective', () => {
  it('should create an instance', () => {
    const directive = new NgTwFormDirective();
    expect(directive).toBeTruthy();
  });
});
