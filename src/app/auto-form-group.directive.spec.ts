import { AutoFormGroupDirective } from './auto-form-group.directive';

describe('AutoFormGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFormGroupDirective();
    expect(directive).toBeTruthy();
  });
});
