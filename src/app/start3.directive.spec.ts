import { Start3Directive } from './start3.directive';

describe('Start3Directive', () => {
  it('should create an instance', () => {
    const directive = new Start3Directive();
    expect(directive).toBeTruthy();
  });
});
